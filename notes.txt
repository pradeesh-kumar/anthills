Features
------------
- Need graceful shutdown for workers?
- Use Logger, SLF4J
- Add worker count for scheduledWorker
- Add micronaut support
- Add spring boot support
- Add workrequest GC support
- Add idempotent operation support
- Add Lease gc
- Create factory methods in AnthillsEngine to create
- Provide Worker insights -> log latency metrics, counters, etc.
- Fix schema for those which doesn't support 'IF NOT EXISTS'
- Feature: Enable custom lease key provider so that clients can provide their own lease key
- Revisit indices in Workrequest table
- Add support for Adaptive lease period based on how long a task takes for each payload class type
- Implement Delayed work request processing logic
- Add delay for next retry
